#include <iostream>
#include <vector>
using namespace std;


void PerformFunc(int r, int c, bool[][] &isVisited){

}

int main(){

    int row, col;
    cin>>row>>col;
    bool[row][col] isVisited={{0}};

    performFunc(3, 4, isVisited)
    // vector<vector<int> > test2D;

    // vector<int> test(5,0);
    // test2D.push_back(test);
    // cout<<test2D.size()<<endl;

    return 0;
}